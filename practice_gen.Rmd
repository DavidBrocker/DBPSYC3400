---
output: pdf_document
---
```{r global_options,echo=FALSE}
knitr::opts_chunk$set(fig.width=12, fig.height=8, comment=NA,warning = FALSE)
```

```{r, echo=FALSE}
x = sample(50:100,10)
y = sample(50:100,10)
xy<-data.frame(x,y)

#write.csv(xy,file="Practice_Sign.csv")
```

## Practice Problem

Look at the following dataset:

```{r, echo=FALSE}
xy
````

Compute the differences of these two scores and make a decision on whether to reject the null that the difference between these two samples is 0. 

